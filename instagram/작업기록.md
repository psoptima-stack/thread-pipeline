# 인스타그램 자동 게시 — 작업 기록

마지막 갱신: 2026-08-21

---

## 1. 지금 어디까지 됐나 (한 줄 요약)

**토큰 발급까지 끝났습니다. 남은 건 GitHub Secret 등록 → 커밋·푸시 → 첫 게시 테스트입니다.**

페이스북 페이지 연결은 Meta 쪽에서 계속 막혀서(`지금은 연결할 수 없습니다`) 포기했고,
페이지를 거치지 않는 **Instagram API with Instagram Login** 방식으로 확정했습니다.

- 계정: `@makesmile999` (BUSINESS)
- `IG_USER_ID` = `17841442009879173`
- Instagram 앱 ID = `1611898950281500` (앱 이름 `smilefb01 - IG`)
- 토큰: 앱 대시보드 → Instagram → API 설정(Instagram 로그인용) → `1. 액세스 토큰 생성`
  → 계정 옆 **토큰 생성** 버튼으로 발급 (60일 유효)
- 워크플로에 `IG_API: instagram` 반영 완료

---

## 2. 만든 파일

| 파일 | 역할 |
|---|---|
| `make_cards.py` | 발행일 원고 → 카드 이미지 (1080×1350, 세이지 톤). **JPEG 출력** |
| `post_to_instagram.py` | 카드를 인스타 캐러셀로 게시. 재시도·중복방지 포함 |
| `check_setup.py` | 토큰/권한/연결 상태 점검 (`--api instagram` 으로 인스타 로그인 토큰 점검) |
| `get_token.py` | 토큰 직접 발급·**60일 갱신**. 대시보드 팝업이 막힐 때 대안 |
| `instagram_config.example.ini` | 설정 예시 (실제 값은 GitHub Secret) |
| `인스타_토큰받는법.md` | 토큰·ID 발급 절차 |
| `../.github/workflows/instagram-daily.yml` | 매일 21:00 KST 자동 실행 |

동작 방식은 기존 `facebook/`, `threads/` 와 동일한 구조입니다.
(발행일정.csv 조회 → 원고 읽기 → 게시 → progress에 기록)

---

## 3. 설계상 꼭 기억할 것 세 가지

### ① 인스타 API는 파일 업로드를 안 받는다
**공개된 https 이미지 주소**만 받습니다. 그래서 카드를 저장소에 커밋해서
`raw.githubusercontent.com` 주소로 넘깁니다. 저장소가 공개라 가능한 방식입니다.
워크플로 순서가 **카드 생성 → 커밋·푸시 → 게시** 인 이유가 이것입니다.
브랜치 주소는 캐시가 남을 수 있어 **커밋 해시로 고정**해서 넘깁니다.

### ② PNG는 거부당한다
게시 API는 JPEG만 받습니다. `make_cards.py` 기본 출력이 jpg입니다.
(`subsampling=0` 으로 저장해야 글자가 안 뭉개집니다)
`--format png` 는 미리보기용이며, cards 아래 png는 `.gitignore` 처리했습니다.

### ③ 캐러셀은 최대 10장
넘으면 앞 10장만 올라가고 경고를 찍습니다. 현재 원고는 5장 내외라 여유 있습니다.

---

## 4. 진행 상황 체크리스트

- [x] 인스타 계정 프로페셔널 전환
- [x] 앱 대시보드에서 인스타그램 테스터 등록
- [x] 인스타에서 테스터 초대 수락
- [x] 토큰 권한 4종 확인 완료
      (`pages_show_list` / `pages_read_engagement` / `instagram_basic` / `instagram_content_publish`)
- [x] 페이지 `Makesmile` (page_id=`1305690905951645`) 인식됨
- [x] ~~페이지 ↔ 인스타 연결~~ → **포기. 인스타 로그인 방식으로 우회**
- [x] 인스타 로그인 토큰 발급 및 검증 (`@makesmile999 (BUSINESS)`)
- [x] 워크플로에 `IG_API: instagram` 반영
- [ ] `IG_USER_ID` / `IG_ACCESS_TOKEN` GitHub Secret 등록
- [ ] 저장소에 커밋·푸시
- [ ] 첫 게시 테스트 (Actions 수동 실행)
- [ ] 60일 토큰 갱신 방식 결정

---

## 5. 페이지 연결을 포기한 이유 (기록)

Meta Business Suite와 페이지 설정 양쪽에서 시도했으나 계속 →

```
지금은 연결할 수 없습니다
페이지에 Instagram 계정을 연결하는 기능을 현재 이용할 수 없습니다.
```

`check_setup.py` 결과도 `✗ 연결된 인스타그램 비즈니스 계정 없음` (부분 연결 흔적도 없음).
3일 뒤 재시도해도 동일. **페이지 경유를 포기하고 인스타 로그인 방식으로 전환.**

> 나중에 페이지 연결이 풀리면 워크플로의 `IG_API: instagram` 을 `facebook` 으로 바꾸고
> 페이지 토큰(만료 없음)을 Secret에 넣으면 그 방식으로 되돌아갈 수 있습니다.

---

## 6. 다음에 할 일

### ① GitHub Secret 등록

저장소 → Settings → Secrets and variables → Actions → New repository secret

| Name | Value |
|---|---|
| `IG_USER_ID` | `17841442009879173` |
| `IG_ACCESS_TOKEN` | 대시보드에서 발급한 토큰 |

### ② 커밋·푸시

로컬에서는 게시 테스트가 **불가능**합니다. 인스타 서버가 카드 이미지를
공개 URL로 내려받아야 하는데 저장소에 올라가 있어야 하기 때문입니다.

### ③ 첫 게시 테스트

GitHub → Actions → **발행일 인스타그램 자동 게시** → Run workflow
→ `date` 에 `2026-08-18` 입력 → 실행 → 로그 확인

### ④ 토큰 갱신 방식 결정 (60일마다 필요)

- **A안**: 워크플로가 매달 자동 갱신 + Secret 자동 업데이트
  → 완전 자동. 단, Secret 쓰기 권한 PAT를 하나 더 만들어야 함
- **B안**: 만료 열흘 전 이메일 알림 → 직접 재발급
  → 간단하지만 손이 감

수동 갱신 명령 (24시간 이상 지난 토큰만 가능):
```powershell
..\.venv\Scripts\python.exe get_token.py refresh --token <기존토큰>
```

---

## 7. 로컬에서 확인하는 법 (토큰 없이도 됨)

```powershell
cd "C:\Users\유나\OneDrive\바탕 화면\writer\instagram"

# 카드 만들기
..\.venv\Scripts\python.exe make_cards.py --date 2026-08-18

# 게시하지 않고 이미지 주소 + 캡션만 확인
..\.venv\Scripts\python.exe post_to_instagram.py --date 2026-08-18 --dry-run
```

검증 완료 기록: 2026-08-18 기준 카드 5장 JPEG 생성, 캡션 387자 정상 출력.

---

## 8. 커밋 상태

아직 **커밋하지 않았습니다.** 변경 목록:

```
M  .gitignore                                  (instagram_config.ini, cards png 제외)
?? .github/workflows/instagram-daily.yml
?? instagram/                                  (폴더 전체)
```

푸시해야 워크플로가 GitHub에서 돌아가고, 카드 이미지도 공개 URL이 생깁니다.

> 비밀값은 올라가지 않습니다. `instagram_config.ini` 는 `.gitignore` 에 있고,
> 토큰은 GitHub Secret에만 들어갑니다.

---

## 9. 게시 시각

- 이메일 / 스레드 / 페이스북: 기존 일정 유지
- 인스타그램: **21:00 KST** (페이스북 20:00과 겹치지 않게 배치)
  변경하려면 `instagram-daily.yml` 의 `cron: '0 12 * * *'` 수정 (UTC 기준)
